rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開發階段：允許已驗證使用者讀寫
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 